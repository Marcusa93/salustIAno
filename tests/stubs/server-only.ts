// Stub de `server-only` para tests con Vitest.
// El paquete real tira un Error al cargarse fuera de un Server Component;
// dentro del runner de tests esa restricción no aplica, así que exportamos
// un módulo vacío.
export {};
